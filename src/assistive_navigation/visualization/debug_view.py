"""
Debug Visualization Module
===========================
Draws debug overlays on video frames using OpenCV.

Phase: 13 (not yet implemented)
Status: STUB — placeholder only

Debug mode overlays (all toggleable via config.yaml):
  - Bounding boxes with class name and confidence
  - Track ID
  - Direction label (LEFT / CENTER / RIGHT)
  - Proximity label (FAR / MEDIUM / CLOSE / VERY CLOSE)
  - Priority score (optional — clutters display)
  - Depth value (optional — for depth debugging)
  - FPS counter
  - System state indicator

Normal mode: no overlay (clean camera view).
Debug mode is enabled via config.yaml: debug.enabled = true
"""

# Implementation begins in Phase 13.
