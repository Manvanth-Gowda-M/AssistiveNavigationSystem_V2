# =============================================================================
# Assistive Navigation System V2 — Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in values if needed.
# The .env file is gitignored and will NOT be committed.
# Most settings are in config/config.yaml — this file is for secrets only.
# =============================================================================

# Currently no secrets are required.
# This file is a placeholder for future needs (e.g., HuggingFace token
# if private model access is ever needed).

# Example (not currently used):
# HUGGINGFACE_TOKEN=your_token_here
