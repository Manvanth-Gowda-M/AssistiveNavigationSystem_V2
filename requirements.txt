# =============================================================================
# Assistive Navigation System V2 — Python Dependencies
# =============================================================================
# These are the EXACT versions installed and tested on:
#   Windows 11, Python 3.10.11, Intel i5-12450H (CPU-only)
#
# To install:
#   .venv\Scripts\activate        (Windows)
#   pip install -r requirements.txt
#
# Phase 1 packages (installed now):
# =============================================================================

# --- Inference backend ---
onnxruntime==1.23.2          # MIT — ONNX model inference (CPU-optimized)

# --- Computer vision ---
opencv-python==5.0.0.93      # Apache-2.0 — frame capture, image processing

# --- Text-to-speech ---
pyttsx3==2.99                # MIT — wraps Windows SAPI5 voices

# --- Performance monitoring ---
psutil==7.2.2                # BSD-3-Clause — CPU/RAM usage measurement

# --- Configuration ---
PyYAML==6.0.3                # MIT — config.yaml loading

# --- Numeric / math (auto-installed as dependencies) ---
numpy==2.2.6                 # BSD-3-Clause — array operations

# --- ONNX Runtime dependencies (auto-installed) ---
coloredlogs==15.0.1          # MIT
flatbuffers==25.12.19        # Apache-2.0
humanfriendly==10.0          # MIT
mpmath==1.3.0                # BSD
packaging==26.3              # Apache-2.0 / BSD
protobuf==7.36.1             # BSD-3-Clause
pypiwin32==223               # PSF (Windows only)
pyreadline3==3.5.6           # BSD
pywin32==312                 # PSF (Windows only)
sympy==1.14.0                # BSD

# --- pyttsx3 dependencies (auto-installed) ---
comtypes==1.4.16             # MIT (Windows COM interface)

# =============================================================================
# Detection + tracking stack (Phase 3+) — INSTALLED and verified.
# These are the exact versions present in the tested .venv (pip freeze).
# Do NOT upgrade/downgrade without re-running the full test + Phase 4/15/16
# evaluation. Ultralytics pulls in torch + torchvision automatically.
# =============================================================================
ultralytics==8.4.137         # AGPL-3.0 — YOLO11 detection + ByteTrack tracking
torch==2.13.0                # BSD-3-Clause — PyTorch (Ultralytics backend, CPU)
torchvision==0.28.0          # BSD-3-Clause — vision ops used by Ultralytics

# =============================================================================
# Depth model (Phase 6+):
#   Depth Anything V2 Small float32 ONNX (models/depth_anything_v2_small.onnx)
#   is downloaded via scripts/download_models.py and run through onnxruntime
#   (already listed above). Model license: Apache-2.0 (Small variant ONLY).
#   No extra Python package is required for depth inference.
#   NOTE: the INT8 depth ONNX variant was evaluated and REJECTED in Phase 6
#   (onnxruntime 1.23.2 does not support its ConvInteger operator). The
#   float32 model is used instead. See docs/models.md.
# =============================================================================

# =============================================================================
# NOT INSTALLED (intentionally) — listed for transparency only:
#   scipy            — was considered for statistical ops but is NOT used and
#                      NOT installed. Do not add it unless a feature needs it.
#   piper-tts-plus   — optional neural-TTS upgrade considered in Phase 11 but
#                      NOT adopted. The system uses pyttsx3 + Windows SAPI5.
# =============================================================================

# --- Testing (added after pytest installed in venv) ---
pytest==9.1.1                # MIT
iniconfig==2.3.0             # MIT
pluggy==1.6.0                # MIT
pygments==2.21.0             # BSD-2-Clause
tomli==2.4.1                 # MIT
exceptiongroup==1.3.1        # MIT
typing-extensions==4.16.0    # PSF
